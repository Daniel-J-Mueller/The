PAGE numeric_loop
PROC main()
    OBJ total = 0
    ITER number intthrough(1, 10000)
        total = total + number
    ITEREND
    RETURN total as result
PROCEND
PAGEEND numeric_loop

ENTRY main
OUT result
